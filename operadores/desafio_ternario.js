let a = 8;
let b = 4;
let operacao = '/';

let resultado = operacao === '+' ? 8 + 4 : operacao === '-' ? 8 - 4 : operacao === '*' ? 8 * 4 : operacao === '/' ? 8 / 4 : 'operador não encontrado';
console.log(resultado);